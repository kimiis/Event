<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Eni-Sortie{% endblock %}</title>
    <link rel="icon"
          href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
    {% block stylesheets %}
    {% endblock %}

    {% block javascripts %}
    {% endblock %}
</head>
<body>
<header>
    {#    si je crée le %block header c'est pour le faire que dans une et une seule page #}

    <div class="titre">
        <h1>Eni-Sortie</h1>
    </div>

    <div class="menuNav">
        <nav class="menu">
            <ul>
                <li>
                    <a href="{{ path('app_home') }}" class="nav">Accueil</a>
                    <a href="{{ path('app_listeEvents') }}" class="nav">Evénements</a>
                    <a href="{{ path('app_create') }}" class="nav">Créer un événement</a>

                    {% if app.user %}

                        <a href="{{ path('app_logout') }}">Connection</a>
                        <a href="{{ path('_profil') }}">Profil</a>

                    {% endif %}

                    {% if not app.user %}

                        <a href="{{ path('app_register') }}">S'enregistrer</a>
                        <a href="{{ path('app_login') }}">Se connecter</a>

                    {% endif %}

                </li>

            </ul>
        </nav>

    </div>


</header>
{% block body %}{% endblock %}
</body>
</html>